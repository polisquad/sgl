#ifndef SGL_ARRAY_H
#define SGL_ARRAY_H

#include "core_types.h"

/**
 * Just a typedef for now.
 * I'm gonna implement this for sure
 */
#include <vector>
template<typename T>
using Array = std::vector<T>;

#endif