#pragma once

#include "core_types.h"

template<typename, typename>		class Array;
template<typename>					class BinaryTree;
template<typename, typename>		class HashMap;
template<typename, typename>		class IMap;
template<typename>					class LinkedList;
template<typename, typename, class>	class Map;
template<typename, typename>		class Pair;
template<typename, typename>		class Queue;
									class String;
template<typename, typename>		class TreeMap;
template<typename, uint32>			class Vector;